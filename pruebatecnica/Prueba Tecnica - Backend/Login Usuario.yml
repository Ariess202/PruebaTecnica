info:
  name: Login Usuario
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/usuarios/login
  body:
    type: json
    data: |-
      {
        "email": "miguelliberato@gmail.com",
        "password": "lvlconsulting123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
