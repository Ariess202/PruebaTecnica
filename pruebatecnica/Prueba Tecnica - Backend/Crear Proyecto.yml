info:
  name: Crear Proyecto
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/proyectos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
