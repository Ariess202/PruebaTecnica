info:
  name: Listar Proyectos de un Usuario (por nombres)
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8080/api/proyectos/home-search?usuarioId=1&nombre=Proyecto
  params:
    - name: usuarioId
      value: "1"
      type: query
    - name: nombre
      value: Proyecto
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
